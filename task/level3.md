# Level 3: Fuzzy Filter (client-side only)

```tsx
<SavvyGrid
  dataset={myData} // Record<string, any>[]
  columns={[{}]} // SavvyColumn[]
  sort={useSortHook()} // manages sort field and direction
  searchValue={""} // string
/>
```
