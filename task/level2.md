# Level 2: Implement sorting based on individual columns

```tsx
function useSortHook() {}

<SavvyGrid
  dataset={myData} // Record<string, any>[]
  columns={[{}]} // SavvyColumn[]
  sort={useSortHook()}
/>;
```
